# Postgres for production/staging (recommended - matches team's likely infra)
DATABASE_URL="postgresql://user:password@localhost:5432/ai_signal?schema=public"

# For quick local dev without a Postgres server, swap the datasource provider
# in prisma/schema.prisma to "sqlite" and use:
# DATABASE_URL="file:./dev.db"

# Temporary auth stub (remove once Module 11 - Auth is merged)
ADMIN_STUB_ROLE="ADMIN"
